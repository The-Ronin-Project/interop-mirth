query (
  $tenantId: String!
  $startDate: String!
  $endDate: String!
  $mrn: String!
) {
  appointmentsByMRNAndDate(
    tenantId: $tenantId
    startDate: $startDate
    endDate: $endDate
    mrn: $mrn
  ) {
    id
  }
}
